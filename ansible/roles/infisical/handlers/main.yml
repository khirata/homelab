---
- name: Restart infisical stack
  ansible.builtin.command:
    cmd: docker compose -f {{ siem_config_dir }}/docker-compose.infisical.yml up -d
  changed_when: true
  ignore_errors: "{{ ansible_check_mode }}"
