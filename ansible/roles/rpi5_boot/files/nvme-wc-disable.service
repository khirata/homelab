[Unit]
Description=Disable NVMe volatile write cache on boot
After=dev-nvme0n1.device
Wants=dev-nvme0n1.device

[Service]
Type=oneshot
ExecStart=/usr/sbin/nvme set-feature /dev/nvme0 -f 6 -v 0
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
