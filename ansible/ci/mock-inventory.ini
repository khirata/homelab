# CI-only inventory — resolves to localhost so templates render without real hosts.

[siem_server]
localhost ansible_connection=local

[postgresql_server]
localhost ansible_connection=local

[redis_server]
localhost ansible_connection=local
